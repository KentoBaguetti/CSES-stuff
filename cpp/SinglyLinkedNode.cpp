#include <bits/stdc++.h>
using namespace std;

template <class LIT>
struct SinglyLinkedNode {
    LIT data;
    SinglyLinkedNode* next;
    SinglyLinkedNode(LIT nData, Node* nx = NULL) : data(nData), next(nx) {}
};

int main () {

return 0;
}
